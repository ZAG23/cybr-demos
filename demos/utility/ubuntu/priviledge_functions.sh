#!/usr/bin/env bash
# Deprecated filename (historical typo). Use privilege_functions.sh for new work.
# shellcheck source=privilege_functions.sh
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/privilege_functions.sh"
