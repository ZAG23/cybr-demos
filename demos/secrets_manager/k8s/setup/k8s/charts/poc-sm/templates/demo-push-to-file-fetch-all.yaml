---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: demo-push-to-file-fetch-all
  name: demo-push-to-file-fetch-all
  namespace: {{ .Values.namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-push-to-file-fetch-all
  template:
    metadata:
      labels:
        app: demo-push-to-file-fetch-all
      annotations:
        conjur.org/container-mode: sidecar
        conjur.org/secrets-destination: file
        conjur.org/jwt-token-path: /var/run/secrets/tokens/jwt
        conjur.org/conjur-secrets.test-app: "*"
        conjur.org/secret-file-path.conjur-demo-file: "./credentials.yaml"
        conjur.org/secret-file-format.conjur-demo-file: "yaml"
        conjur.org/secrets-refresh-enabled: "true"
        conjur.org/secrets-refresh-interval: 60s
        conjur.org/remove-deleted-secrets-enabled: "true"
    spec:
      serviceAccountName: {{ .Values.sm_app_service_account }}
      containers:
        - name: app-container
          image: alpine:latest
          command: [ "/bin/sh", "-c", "--" ]
          args: [ "while true; do sleep 30; done;" ]
          volumeMounts:
            - name: conjur-secrets
              mountPath: /opt/secrets/conjur
              readOnly: false
        - name: cyberark-secrets-provider-for-k8s
          image: 'cyberark/secrets-provider-for-k8s:latest'
          imagePullPolicy: Always
          volumeMounts:
            - name: podinfo
              mountPath: /conjur/podinfo
            - name: conjur-secrets
              mountPath: /conjur/
            - name: jwt-token
              mountPath: /var/run/secrets/tokens
          env:
            - name: MY_POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          envFrom:
            - configMapRef:
                name: sm-configmap
      volumes:
        - name: podinfo
          downwardAPI:
            items:
              - path: "annotations"
                fieldRef:
                  fieldPath: metadata.annotations
        - name: conjur-secrets
          emptyDir:
            medium: Memory
        - name: jwt-token
          projected:
            sources:
              - serviceAccountToken:
                  path: jwt
                  expirationSeconds: 6000
                  audience: conjur
